package components.nanofl.others.custompropertiespane.item_info;

class Code extends wquery.Component
{
    var label = "";
    
    var title = ""; // unused, just to prevent wquery warning
    var units = ""; // unused, just to prevent wquery warning
}